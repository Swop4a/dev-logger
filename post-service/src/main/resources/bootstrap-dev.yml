spring:
  jackson:
    mapper:
      default-view-inclusion: true
  data:
    mongodb:
      host: localhost
      port: 27017
      username: admin
      password: admin
      database: admin
  cloud:
    config:
      enabled: false
server:
  port: 8085
  context-path: /posts
eureka:
  client:
    enabled: false